version: "1.0"
linter: jetbrains/qodana-jvm:2025.1
profile:
  path: .qodana/profiles/qodana-config.xml

include:
  - name: All
coverage:
  reportProblems: true
  exclusions:
    - name: "Ignore build outputs"
      paths:
        - "**/build/**"
        - "**/out/**"